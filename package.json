{
  "name": "plru-cache",
  "version": "0.1.0",
  "description": "LRU Cache, but persistent.",
  "main": "dist/src/index.js",
  "scripts": {
    "test": "npm run purge && npm run compile && jest",
    "compile": "tsc",
    "purge": "rimraf dist"
  },
  "author": "Vajahath Ahmed <vajuoff.1@gmail.com>",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "@types/node": "^10.5.8",
    "jest": "^23.5.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.0.1"
  }
}
